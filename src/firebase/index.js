import firebase from "firebase/app";
import "firebase/storage";
const firebaseConfig = {
    // Ban them config vao day


};

// Initialize Firebase
firebase.initializeApp(firebaseConfig);
export default firebase;